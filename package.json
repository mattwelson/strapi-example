{
  "name": "dtl",
  "private": "true",
  "scripts": {
    "clear": "cd apps/frontend && rm -rf .next && rm -rf cache",
    "export": "cd apps/backend && yarn strapi export --no-encrypt -f ../../seed-data",
    "seed": "cd apps/backend && yarn strapi import -f ../../seed-data.tar.gz"
  },
  "dependencies": {
    "npm-check-updates": "^16.14.15",
    "turbo": "^2.0.6"
  },
  "packageManager": "pnpm@9.2.0"
}
